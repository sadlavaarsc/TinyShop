server:
  port: 8081
spring:
  application:
    name: user-service
  datasource:
    url: jdbc:mysql://localhost:3306/tinyshop_user?useUnicode=true&characterEncoding=utf-8
    username: root
    password: root
